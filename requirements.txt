# GUI
customtkinter==5.2.2

# HTTP Client
httpx==0.27.2  # Pinned to 0.27.x for mistralai compatibility
chardet==5.2.0  # Character encoding detection for web scraping
brotli==1.2.0   # Brotli decompression for web content

# LLM Providers
anthropic==0.40.0  # Claude API (claude-3-5-sonnet, claude-3-opus, claude-3-haiku)
google-generativeai==0.8.3  # Gemini API (gemini-1.5-pro, gemini-1.5-flash)
mistralai==1.2.4  # Mistral AI API (mistral-large, mistral-small)

# Vector Database & Embeddings
chromadb==1.3.4  # Modern ChromaDB with no multiprocessing (fixes semaphore leaks!)
sentence-transformers==5.1.2
rank-bm25==0.2.2  # BM25 algorithm for keyword-based search

# LangChain ecosystem - Modern modular architecture
langchain==1.0.5  # Main LangChain package
langchain-core==1.0.4  # Core abstractions and base classes
langchain-chroma==1.0.0  # ChromaDB integration
langchain-text-splitters==1.0.0  # Text splitting utilities
langchain-huggingface==1.0.1  # HuggingFace embeddings integration
langchain-community==1.0.5  # Community-contributed retrievers (BM25, etc.)

# Data validation
pydantic==2.12.4

# Web scraping and content extraction
browser-cookie3==0.20.1  # Extract cookies from browser for authenticated requests
readability-lxml==0.8.4.1  # Extract main article content from HTML
markdownify==1.2.0  # Convert HTML to Markdown
lxml==6.0.2  # Required by readability-lxml
beautifulsoup4==4.14.2  # HTML parsing utilities

# NOTE: marker-pdf requires separate installation due to heavy ML dependencies
# Run: python setup_marker.py
# Or manually: pip install marker-pdf
